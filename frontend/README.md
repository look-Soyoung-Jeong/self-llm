# frontend

React + Vite frontend for the local LLM starter.

## 실행

```bash
cd frontend
npm install
npm run dev
```

## 환경

`frontend` 앱은 기본적으로 `http://localhost:8000/api`를 호출하도록 구성되어 있습니다.

필요 시 `VITE_API_BASE` 환경 변수를 사용하여 백엔드 URL을 조정하세요.
